#include "../internal/app/finance_app.h"

/**
 * @brief Главная функция для запуска финансового менеджера.
 *
 * Вызывает функцию `run_finance_app` для инициализации и запуска приложения.
 *
 * @return Код завершения работы приложения.
 */
int main() {
    return run_finance_app();
} 