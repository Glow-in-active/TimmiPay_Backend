#ifndef FINANCE_APP_H
#define FINANCE_APP_H

/**
 * @brief Запускает основную логику финансового приложения.
 *
 * Инициализирует необходимые компоненты и запускает сервер.
 *
 * @return Код завершения: 0 при успехе, 1 при ошибке.
 */
int run_finance_app();

#endif  // FINANCE_APP_H